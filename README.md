# Simple Angular Todo With NgRx

**1. Run npm install** <br> `npm i`

**3. install json-server** <br> `npm install -g json-server`

**4. start backend** <br> `json-server --watch db.json`

**5. start client** <br> `ionic serve`

